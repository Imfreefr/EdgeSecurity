async function initActivities(){try{const data=await EdgeAPI.get('/activities'),s=EdgeAuth.current();document.getElementById('activity-stats').innerHTML=[['Usuários cadastrados',EdgeDB.users.length,''],['Usuário atual',s.nome,'Online'],['Ações registradas',data.length,'SQLite'],['Câmeras permitidas',EdgeDB.cameras.length,'']].map(x=>`<div class="stat"><div class="label">${x[0]}</div><div class="value" style="font-size:${typeof x[1]==='string'?'18px':'26px'}">${x[1]}</div><div class="hint">${x[2]}</div></div>`).join('');document.getElementById('activities-table').innerHTML=data.length?`<table><thead><tr><th>Ação</th><th>Descrição</th><th>Data</th></tr></thead><tbody>${data.map(a=>`<tr><td>${escapeHtml(a.acao)}</td><td>${escapeHtml(a.descricao)}</td><td>${formatDate(a.data_hora)}</td></tr>`).join('')}</tbody></table>`:'<div class="empty">Nenhuma atividade registrada.</div>'}catch(e){showToast(e.message)}}window.addEventListener('edge-data-ready',initActivities);
